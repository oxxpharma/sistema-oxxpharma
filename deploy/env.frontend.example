# ============================================================
# Frontend - OxxPharma .env (PRODUCAO)
# ============================================================
# A URL do backend para o frontend chamar. Como nginx faz proxy /api -> :8001,
# basta apontar para o proprio dominio.
REACT_APP_BACKEND_URL=https://__DOMAIN__
