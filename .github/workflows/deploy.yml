name: Build & Deploy to Production

on:
  push:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------- Frontend build ----------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install frontend deps
        working-directory: frontend
        run: |
          if [ -f yarn.lock ]; then
            yarn install --frozen-lockfile
          else
            yarn install
          fi

      - name: Build frontend
        working-directory: frontend
        env:
          # A URL do backend usada pelo build de produção. Configure em Settings > Secrets.
          REACT_APP_BACKEND_URL: ${{ secrets.REACT_APP_BACKEND_URL }}
          CI: "false"  # evita quebrar build por warnings do CRA
        run: yarn build

      # ---------- Backend prepara (sem build; Python só copia) ----------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate backend syntax
        working-directory: backend
        run: |
          python -m compileall -q .

      # ---------- Cria pacote de deploy ----------
      - name: Prepare release bundle
        run: |
          mkdir -p release/backend release/frontend
          # Backend: copia tudo exceto __pycache__ e uploads temporários
          rsync -a --exclude='__pycache__' --exclude='*.pyc' --exclude='uploads' \
                --exclude='.env' backend/ release/backend/
          # Frontend: apenas o build já compilado + package.json p/ referência
          cp -r frontend/build release/frontend/build
          cp frontend/package.json release/frontend/
          # Manifests úteis pra debug
          echo "commit=${GITHUB_SHA}" > release/RELEASE.txt
          echo "built_at=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> release/RELEASE.txt
          echo "ref=${GITHUB_REF}" >> release/RELEASE.txt

      - name: Show release size
        run: du -sh release/backend release/frontend/build

      # ---------- Deploy via SSH (com senha usando sshpass) ----------
      - name: Install sshpass
        run: sudo apt-get update -qq && sudo apt-get install -y -qq sshpass rsync

      - name: Deploy files to production server
        env:
          SSHPASS: ${{ secrets.SSH_PASSWORD }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/var/www/oxxpharma' }}
        run: |
          DEST_ROOT="${DEPLOY_PATH%/}"
          SSH_OPTS="ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p ${SSH_PORT}"

          echo "→ Sincronizando backend/ para ${DEST_ROOT}/backend/"
          sshpass -e rsync -avz \
            --exclude='.env' --exclude='uploads' --exclude='__pycache__' --exclude='*.pyc' \
            -e "${SSH_OPTS}" \
            release/backend/ "${SSH_USER}@${SSH_HOST}:${DEST_ROOT}/backend/"

          echo "→ Sincronizando frontend/build/ para ${DEST_ROOT}/frontend/build/"
          sshpass -e rsync -avz --delete \
            -e "${SSH_OPTS}" \
            release/frontend/build/ "${SSH_USER}@${SSH_HOST}:${DEST_ROOT}/frontend/build/"

          echo "→ Copiando RELEASE.txt"
          sshpass -e rsync -avz \
            -e "${SSH_OPTS}" \
            release/RELEASE.txt "${SSH_USER}@${SSH_HOST}:${DEST_ROOT}/RELEASE.txt"

      - name: Restart services on remote
        env:
          SSHPASS: ${{ secrets.SSH_PASSWORD }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/var/www/oxxpharma' }}
          BACKEND_PROCESS: ${{ secrets.BACKEND_PROCESS || 'oxxpharma-backend' }}
        run: |
          sshpass -e ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -p "${SSH_PORT}" "${SSH_USER}@${SSH_HOST}" bash -s <<REMOTE_EOF
          set -e
          cd "${DEPLOY_PATH}"

          # 1) Instala/atualiza deps do backend dentro do venv
          if [ -d venv ] && [ -f backend/requirements.txt ]; then
            echo "→ Instalando deps no venv..."
            ./venv/bin/pip install -q -r backend/requirements.txt
          else
            echo "⚠ Venv não encontrado em ${DEPLOY_PATH}/venv — pulei instalação de deps"
          fi

          # 2) Reinicia o backend
          echo "→ Reiniciando ${BACKEND_PROCESS}..."
          sudo supervisorctl restart "${BACKEND_PROCESS}"

          # 3) Frontend é estático (nginx serve /frontend/build) — nada pra reiniciar

          # 4) Healthcheck
          sleep 3
          if curl -fsS http://localhost:8001/api/health >/dev/null; then
            echo "✅ Backend respondendo"
          else
            echo "⚠ Backend health check falhou — confira: sudo supervisorctl tail -f ${BACKEND_PROCESS}"
          fi
          REMOTE_EOF

      - name: Notify success
        if: success()
        run: echo "✅ Deploy concluído com sucesso · commit ${GITHUB_SHA:0:7}"
